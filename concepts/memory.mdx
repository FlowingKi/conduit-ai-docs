---
title: Memory
description: Sistema de memoria persistente de Conduit AI — hechos, notas, carpeta local y export.
---

Memory es el sistema de memoria persistente de Conduit AI. Los agentes pueden leer y escribir hechos, notas y contexto que persiste entre sesiones.

## Tipos de memoria

### Hechos

Los hechos son piezas de información cortas y estructuradas que los agentes extraen de las conversaciones. Se guardan en la base de datos y están disponibles en todas las sesiones futuras.

Ejemplos de hechos:
- `El proyecto usa TypeScript 5.4`
- `El usuario prefiere respuestas en español`
- `La API de producción está en api.ejemplo.com`

**Límite de hechos por workspace**: configurable (default 10). Cuando se llega al límite, los hechos más antiguos se desplazan para dar lugar a los nuevos.

### Notas

Las notas son documentos más largos creados por los agentes o el usuario. Se guardan en `MEMORY/notes/` (storage local) o en cloud.

**Destilar sesión → nota**: en la pestaña Memory de un workspace, podés solicitar que el agente destile la sesión actual en una nota de resumen. La nota queda en `MEMORY/notes/`.

### Contexto de sesión

El contexto de sesión captura información relevante de la conversación actual para que los agentes puedan retomarlo en el futuro.

## Configuración

Desde **Memory** en el menú lateral o desde la pestaña **Memory** de un workspace:

| Opción | Descripción |
|--------|-------------|
| **Storage** | Dónde se guardan los datos — ver opciones abajo |
| **Límite de hechos** | Cuántos hechos se retienen (5, 10, 20, 50) |
| **Retención** | Por cuánto tiempo se conservan los hechos (30d, 90d, 1 año) |

### Opciones de storage

| Opción | Descripción |
|--------|-------------|
| **Cloud Conduit** | Supabase — accesible desde cualquier dispositivo, sin configuración extra |
| **Local .md** | Archivos Markdown en una carpeta local de tu máquina, vía el bridge |
| **Local .json** | Igual que `.md` pero en formato JSON |
| **Local SQLite** | Base de datos SQLite local |

Para usar storage local, el bridge tiene que estar corriendo y tenés que configurar la ruta de la carpeta en **Settings** del workspace (campo **Carpeta local**). Funciona con cualquier carpeta en disco, incluyendo un vault de Obsidian.

### Cambiar de storage — wizard de migración

Al cambiar de storage aparece un wizard que te guía paso a paso para configurar la nueva ubicación y opcionalmente migrar los datos existentes.

## Sync con carpeta local

Si configuraste storage local, las notas creadas vía "Destilar sesión" aparecen en `MEMORY/notes/` dentro de tu carpeta. Podés leerlas y editarlas directamente — los cambios los lee el agente la próxima vez que los consulte.

La pestaña Memory del workspace muestra el árbol de archivos con `CONTEXT/` y `MEMORY/` como nodos raíz. Podés navegar, crear y eliminar archivos y carpetas desde ahí.

## Export de memoria

Desde la pestaña Memory de un workspace, podés exportar todos los hechos del workspace a un archivo markdown. El archivo se guarda con el nombre `MEMORY/memoria-FECHA.md`.

Útil para:
- Backup antes de limpiar hechos
- Compartir contexto con otra persona
- Revisión manual del estado de la memoria

## Triggers de memoria automática

Los agentes pueden capturar hechos automáticamente según estos disparadores (configurables por workspace):

| Trigger | Cuándo actúa |
|---------|--------------|
| Sprint | Al inicio o fin de un sprint |
| End of day | Al cerrar el workspace al final del día |
| Session 20% | Cuando el contexto de sesión está al 20% restante |
| Keywords | Al detectar palabras clave específicas |
| Interval | Cada N minutos de conversación |
